name: E2E Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:
# E2E jobs are commented out — requires native build environment (Xcode/Android Studio).
# Uncomment when native iOS/Android project files are set up.

# jobs:
#   e2e-ios:
#     name: E2E — iOS Simulator
#     runs-on: macos-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Install CocoaPods
#         working-directory: ios
#         run: pod install
#
#       - name: Build iOS app for E2E
#         run: npm run build:e2e:ios
#
#       - name: Run E2E tests (iOS)
#         run: npm run test:e2e:ios
#
#   e2e-android:
#     name: E2E — Android Emulator
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm
#
#       - name: Setup Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: 17
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Enable KVM
#         run: |
#           echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#           sudo udevadm control --reload-rules
#           sudo udevadm trigger --name-match=kvm
#
#       - name: Build Android app for E2E
#         run: npm run build:e2e:android
#
#       - name: Run E2E tests (Android)
#         uses: reactivecircus/android-emulator-runner@v2
#         with:
#           api-level: 34
#           target: google_apis
#           arch: x86_64
#           profile: pixel_7
#           script: npm run test:e2e:android
